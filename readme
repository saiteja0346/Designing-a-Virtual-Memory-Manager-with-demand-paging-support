Save the code that give below VirtuaMM.java code and run the code then after run the code u will get interface 
in that interfce starting you will get physical memory then you shhould enter physical memory after you will get the vitural memory after you enter virtural memory you can access to the inter 
note that virtual memory should less then the physical memory 
